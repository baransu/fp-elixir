a = 1 + 1 - 2 / 10 * 0

sum = [1, 2, 3] ++ [4, 5, 6]
diff = [1, 2, 3, 4, 5] -- [1, 5]

string = "first" <> "_" <> "second"

bool = true and false or not true

oter_bool = 1 && :atom || !false

==, !=, ===, !==, <=, >=, <, >

true = 1 == 1.0
false = 1 === 1.0
